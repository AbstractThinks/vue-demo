import recommendmor from './Morning.js';

// initial state
const state = {
  ...recommendmor.state,
}

// getters
const getters = {
  ...recommendmor.getters,
}

const actions = {
  ...recommendmor.actions,
}

// mutations
const mutations = {
  ...recommendmor.mutations,
}

export default {
  state,
  getters,
  actions,
  mutations
}
